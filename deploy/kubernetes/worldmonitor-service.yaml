apiVersion: v1
kind: Service
metadata:
  name: worldmonitor
  labels:
    app.kubernetes.io/name: worldmonitor
    app.kubernetes.io/part-of: worldmonitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: worldmonitor
  ports:
    - name: http
      port: 8080
      targetPort: http
