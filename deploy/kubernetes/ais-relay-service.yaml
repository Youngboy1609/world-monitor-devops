apiVersion: v1
kind: Service
metadata:
  name: ais-relay
  labels:
    app.kubernetes.io/name: ais-relay
    app.kubernetes.io/part-of: worldmonitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ais-relay
  ports:
    - name: http
      port: 3004
      targetPort: http
